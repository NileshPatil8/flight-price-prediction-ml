# flight-price-prediction-ml
 Machine Learning project that predicts flight ticket prices using the Clean Flight Dataset. The project includes data preprocessing, feature engineering, one-hot encoding,  Random Forest regression model training, performance evaluation (MAE, MSE, RMSE, R²),  visualization of actual vs predicted prices, and saving the trained model for deployment.
